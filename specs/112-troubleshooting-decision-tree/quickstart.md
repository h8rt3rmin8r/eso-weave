# Quickstart: Verify the Troubleshooting Decision Tree

1. Run the focused documentation policy and browser receipt tests.
2. Build and test the mdBook, then run link checks.
3. Run the complete generated-site policy against the built book.
4. Run the one-process browser smoke and confirm 40 rendering observations plus five layout observations.
5. Inspect the source page and generated page in navy and light at 320 and 1280 CSS pixels.
6. Inspect the shared expanded view at 200 percent browser zoom and verify every label remains readable and reachable.
7. Compare the five match endpoints and unmatched continuation with the complete troubleshooting prose.
8. Confirm the SVG has no external resources or active content and the generated bytes match source.
9. Run spelling, encoding, newline, forbidden-dash, mojibake, trust, diff, and repository gates.

Expected result: one accessible local decision tree routes five symptom families to their next evidence checks, the complete prose remains authoritative, and all source, generated, rendering, layout, and figure-system receipts pass.
