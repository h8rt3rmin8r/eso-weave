# Quickstart: Verify S094

1. Run the focused Lua and import contract tests and confirm every subscribed
   callback, tagged scalar, unknown value, loss path, and mixed-version store.
2. Run metric, recommendation, history, and data-addon regressions.
3. Run the complete local merge gate from `docs/project/build-autopilot.md`.
4. Inspect generated SavedVariables fixtures to confirm raw values are synthetic,
   raw sequences reconcile, and v1 fixtures remain unchanged.
5. Verify the PR has green hosted checks, no unresolved review thread, and no more
   than one explicit second-round `@Codex review` request.

Expected user behavior remains `/ewencounter arm live|pts`, one combat, flush ESO
SavedVariables, then explicit desktop import. The arm message now warns that exact
local callback values can include names and identifiers.

