# Screenshot Plan: Data Addon Lifecycle UI

Refresh every affected System and State capture in both themes and the compact
and wide viewport families. Seed the managed current package for established
dashboard scenes and keep first-launch or unavailable scenes explicit.

Required visible states are managed current with separate unconfirmed facts and
missing with Install guidance. No capture may display a private path, account
name, character name, or obsolete Collector package name.

Run the repository documentation-capture test into a temporary target directory,
inspect all changed image files, then update canonical manifest captions, alt text,
dimensions, byte counts, and hashes.
